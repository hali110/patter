# Revisions to skip in git blame. Wire it up once per clone:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only whitespace-only, tree-wide reformats belong here. Each entry must be
# verified semantically inert before being added - for the commit below, both
# files were byte-identical after stripping all whitespace and the result
# compiled clean. Never add a commit that changed behaviour: this file makes
# blame lie about it permanently.

# Apply swift-format to the daemon (whitespace only)
00676102ab15714958ba2c0d9adde55513a37150
